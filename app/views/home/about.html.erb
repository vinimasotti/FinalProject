<div class="music-platform-header">
  <div class="header-content">
    <hr class="header-divider">
    <h2 class="platform-title">Secure Online Music Storage Platform</h2>
    <hr class="header-divider">
    
    <h1 class="welcome-title">Welcome to <span class="brand-name">AudioGram</span></h1>
    <hr class="header-divider">
    
    <p class="platform-description">
      A friendly home for music lovers - store your personal collection safely, 
      discover hidden gems, and connect with others. <br>
      Enjoy <span class="highlight">invisible security</span> that works while 
      you focus on what matters: <span class="emphasis">the music</span>.
    </p>
  </div>
</div>

<div class="text-center">
  <%= link_to "Log In", new_user_session_path, class: "btn btn-primary btn-lg rounded-pill shadow-sm px-5 py-3 mb-3", style: "border: none; transition: transform 0.2s; font-size: 1.5rem;" %>
  <br>
  <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-lg rounded-pill shadow-sm px-5 py-3 mb-3", style: "background: linear-gradient(135deg, #34495e, #7f8c8d); border: none; color: white; transition: transform 0.2s; font-size: 1.5rem;" %>
</div>



<style>
  .btn:hover {
  transform: scale(1.05); 
 }

  .music-platform-header {
    font-family: 'Segoe UI', Roboto, sans-serif;
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem 1.5rem;
    color: #333;
  }

  .header-content {
    text-align: center;
  }

  .header-divider {
    border: 0;
    height: 1px;
    background: linear-gradient(to right, transparent, #666, transparent);
    margin: 1.5rem auto;
    width: 70%;
  }

  .platform-title {
    font-size: clamp(1.3rem, 3vw, 1.8rem);
    font-weight: 400;
    color: #555;
    margin: 1rem 0;
  }

  .welcome-title {
    font-size: clamp(2rem, 6vw, 3.5rem);
    font-weight: 700;
    margin: 1rem 0;
    color: #222;
  }

  .brand-name {
    color: #0066cc;
    background: linear-gradient(135deg, #0066cc, #00aaff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .platform-description {
    font-size: clamp(1rem, 2.5vw, 1.2rem);
    line-height: 1.6;
    margin: 2rem auto;
    max-width: 700px;
    color: #444;
  }

  .highlight {
    font-weight: 600;
    color: #0066cc;
  }

  .emphasis {
    font-style: italic;
    color: #00aaff;
  }

  @media (max-width: 768px) {
    .header-divider {
      width: 90%;
      margin: 1rem auto;
    }
    
    .platform-description {
      padding: 0 1rem;
    }
  }
 
</style>